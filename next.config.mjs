/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  i18n: {
    locales: ['en', 'cs'],
    localeDetection: true,
    defaultLocale: 'en',
  },
};

  

export default nextConfig;
